FROM node:22-slim

# Install Claude Code CLI
RUN npm install -g @anthropic-ai/claude-code

# Create workspace and home directory for non-root user
RUN mkdir -p /workspace /home/node/.claude && chown -R node:node /home/node /workspace

WORKDIR /workspace

# Default entrypoint is the claude CLI
ENTRYPOINT ["claude"]
